# 食規雷達 操作手冊

> 這份是日常使用速查。從 0 開始的部署看 [部署步驟.md](./部署步驟.md)，git 推送設定看 [設定git推送.md](./設定git推送.md)。

---

## 🌐 重要網址

| 用途 | 網址 |
|---|---|
| 公開網站（給衛生管理員看） | https://mickcheng2026.github.io/foodreg-radar/ |
| GitHub repo（管理程式碼） | https://github.com/mickcheng2026/foodreg-radar |
| GitHub Actions（看自動排程紀錄）| https://github.com/mickcheng2026/foodreg-radar/actions |
| PAT 管理頁 | https://github.com/settings/tokens |

---

## 🎯 最常用的 3 種情境

### A. 跟 Claude Code 對話（最輕鬆）

直接在對話框打：

| 您要做的事 | 對 Claude Code 說 |
|---|---|
| 把最新公告抓下來、產 AI 重點、上線 | **「FoodReg 抓新資料推送」** |
| 只幫已有的舊資料補 AI 重點 | **「FoodReg 補摘要」** |
| 幫某幾筆特定公告補摘要 | **「FoodReg 幫第 5 筆和第 8 筆補摘要」** |
| 改網站名稱、配色等 | **「FoodReg 把標題改成 ___」** |
| 增加新資料來源 | **「FoodReg 加 ___ 來源」**（CAS、農業部等）|

我會自動做完上述所有事 + 推上 GitHub，您 1-2 分鐘後重新整理網站就會更新。

### B. 自己跑終端機（不用 Claude Code 也行）

打開**終端機 (Terminal)**，輸入：

```bash
cd "/Users/mick/Desktop/claude code files/teamnotes-vault/條文自動更新 網站"

# 推送現有本機變更
./推送.sh

# 先抓最新公告再推送（會跑爬蟲約 60-90 秒）
./推送.sh --refresh
```

### C. 完全不管它（讓系統自動跑）

什麼都不做。GitHub Actions **週一到週五 台灣時間早上 7:50** 會自動：
1. 跑爬蟲抓食藥署、EFSA 等最新公告
2. 更新 `data/items.json`
3. 自動 commit 推回 GitHub
4. 網站幾分鐘內更新

⚠️ **限制**：自動排程的這條路**不會生成 AI 摘要**（因為沒設 API key），只會更新原文摘要。要有 AI 重點還是得用 A 或 B 方式。

---

## 📊 怎麼確認網站有更新

1. 打開 https://mickcheng2026.github.io/foodreg-radar/
2. **強制重新整理**：⌘ + Shift + R（繞過快取）
3. 確認三件事：
   - 頂部「最後更新」時間是不是今天
   - 卡片列表第一張的日期是不是最新
   - 有 AI 摘要的卡片是否顯示綠色「AI 重點」標籤

## 🤖 怎麼確認自動排程有跑

1. 打開 https://github.com/mickcheng2026/foodreg-radar/actions
2. 看左側「每日自動更新食規資料」
3. 點進去 → 看最近一次執行
   - 綠色勾 ✅ = 正常
   - 紅色叉 ❌ = 有錯，可以點進去看 log

## ⚠️ 常見問題

### Q1：網站打開卡在「載入資料中」永遠不動
**原因**：可能是 GitHub Pages 還在部署，或瀏覽器快取舊版
**解法**：等 3 分鐘後，⌘ + Shift + R 強制重新整理

### Q2：推送顯示 `Updates were rejected`
**原因**：線上有改動本機沒同步
**解法**：終端機跑 `git pull origin main` 再跑 `./推送.sh`

### Q3：推送顯示 `refusing to allow ... without workflow scope`
**原因**：PAT 沒勾 `workflow` 權限
**解法**：去 https://github.com/settings/tokens → 點 `foodreg` → 勾 `workflow` → Update token → 重跑 `./推送.sh`

### Q4：推送顯示 `Authentication failed`
**原因**：PAT 過期或被撤銷
**解法**：重做一次 [設定git推送.md](./設定git推送.md) 的步驟 1（建新 PAT）；然後跑 `./推送.sh` 重新輸入

### Q5：GitHub Actions 紅色叉，網站好幾天沒更新
**原因**：可能是食藥署 / EFSA 網站結構改了，爬蟲失敗
**解法**：跟 Claude Code 說「**FoodReg 爬蟲壞了**」，我會去看 log + 修

### Q6：想加新的資料來源（CAS、農業部、各縣市衛生局…）
跟 Claude Code 說「**FoodReg 加 CAS 來源，網址：xxx**」

### Q7：想改網站名稱、Logo、配色
跟 Claude Code 說「**FoodReg 把名稱改成 ___**」、「**配色改成深色系**」等

### Q8：衛生管理員回饋想加某功能（按收藏、Line 通知…）
告訴我，多數功能 30 分鐘內可加上。

---

## 🔐 安全規則

| ✅ 可以跟 Claude 講 | ❌ 永遠不要貼給 Claude |
|---|---|
| 錯誤訊息 / 截圖 | 密碼、PAT (`ghp_...`)、API key |
| GitHub 帳號名稱 | `.env` 檔案內容 |
| 想做的功能描述 | 信用卡資訊 |
| 程式碼 | 含 token 的網址（`?token=xxx`）|

如果不小心貼了 → **立刻去 GitHub 撤銷該 PAT**（30 秒），然後重建。

---

## 📁 專案重要檔案位置

| 檔案 | 用途 | 要不要動 |
|---|---|---|
| `index.html` | 網站前端 | 要改外觀才動 |
| `data/items.json` | 所有抓到的資料 | **自動產生，不要手動編輯** |
| `scripts/build_data.py` | 主爬蟲總控 | 加來源時動 |
| `scripts/source_tfda.py` | 食藥署爬蟲 | TFDA 改版時動 |
| `scripts/source_efsa.py` | EFSA 爬蟲 | EFSA 改版時動 |
| `scripts/apply_ai_summaries.py` | AI 摘要對應表 | 加新摘要時動 |
| `推送.sh` | 一鍵推送 | 不用動 |
| `.github/workflows/update-data.yml` | 自動排程設定 | 改排程時動 |

---

## 💰 費用總覽

| 項目 | 每月成本 |
|---|---|
| GitHub Pages（網站託管）| $0 |
| GitHub Actions（自動排程）| $0（免費額度遠遠用不完）|
| AI 摘要（透過 Claude Code Pro）| 已含在訂閱裡 |
| 網域名稱 | $0（用 `.github.io`）|
| **總計** | **$0 額外費用** |

如果未來想用自訂網域（例如 `foodreg.tw`），網域註冊費約 NT$500/年。

---

## 🚀 進階：未來可以加的功能

當您準備好擴張時，告訴 Claude Code：

- **「FoodReg 加 LINE Notify」** — 有新公告自動推播到 LINE
- **「FoodReg 加 Email 訂閱」** — 衛生管理員留 email 就可以收每週彙整
- **「FoodReg 加 RSS feed」** — 讓進階使用者用 Feedly 訂閱
- **「FoodReg 加標籤分類」** — HACCP / 添加物 / 標示 / 農藥 分類瀏覽
- **「FoodReg 加搜尋紀錄」** — 顯示熱門搜尋關鍵字
- **「FoodReg 加 Google Analytics」** — 知道有多少人在用
- **「FoodReg 改成深色模式」** — 切換亮暗主題

每項都是 0 元方案，30 分鐘內可上線。

---

最後更新：2026-05-30（全面 8 國家 + 427 筆完整版）
